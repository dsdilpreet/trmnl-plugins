{% assign chart = videosDaily | default: videosWeekly %}
<div class="layout layout--col layout--top">
  {% if chart and chart.videoViews %}
    <div class="label lg:label--large" style="margin-bottom: 6px;">Top Music Videos</div>
    <div class="flex flex--col" data-overflow="true">
      {% for video in chart.videoViews %}
          <div class="item" style="margin-bottom: 8px;">
            <div class="icon" style="width: 80px; min-width: 80px; align-self: stretch;">
            {% if video.thumbnailUrl %}
              <img class="image image--cover image-dither" style="width: 100%; height: 100%;" src="{{ video.thumbnailUrl }}">
            {% endif %}
          </div>
          <div class="content">
            <span class="title title--small">{{ video.title | default: "Unknown" | truncate: 34 }}</span>
            <span class="description">{{ video.artists | default: "Unknown" | truncate: 42 }}</span>
            <div class="flex gap--small">
              <span class="label label--small">{{ video.viewCount | default: "-" }} views</span>
              <span class="label label--small">{{ video.percentViewsChange | default: "-" }}</span>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  {% else %}
    <div class="layout layout--col layout--center">
      <span class="value value--small">No data</span>
    </div>
  {% endif %}
</div>

<div class="title_bar">
  <img class="image image--adaptive" src="https://trmnl.com/images/plugins/trmnl--render.svg">
  <span class="title">{{ trmnl.plugin_settings.instance_name }} • {{ country.name | default: "Global" }}</span>
  <span class="instance">{{ lastUpdatedLocalTime | date: "%b %d, %I:%M %p" }}</span>
</div>
